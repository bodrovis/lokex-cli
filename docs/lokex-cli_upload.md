## lokex-cli upload

Upload translation files to Lokalise

```
lokex-cli upload [flags]
```

### Options

```
      --manifest string                           Path to JSON manifest for batch upload
      --filename string                           Filename sent to Lokalise (required)
      --src-path string                           Local path to read file contents from (optional)
      --data string                               Base64-encoded file contents (optional; if set, file is not read from disk)
      --lang-iso string                           Language code of the translations in the file (required)
      --poll                                      Wait until Lokalise finishes processing the upload
      --format string                             File format (e.g. json, strings, xml)
      --tags strings                              Tags to apply to keys
      --custom-translation-status-ids strings     Custom translation status IDs to add
      --convert-placeholders                      Convert placeholders to Lokalise universal placeholders
      --detect-icu-plurals                        Automatically detect and parse ICU plurals
      --tag-inserted-keys                         Add tags to inserted keys
      --tag-updated-keys                          Add tags to updated keys
      --tag-skipped-keys                          Add tags to skipped keys
      --replace-modified                          Replace modified translations from the uploaded file
      --slashn-to-linebreak                       Replace \n with a real line break
      --keys-to-values                            Replace values with key names
      --distinguish-by-file                       Allow same key names to coexist across different filenames
      --apply-tm                                  Apply 100% translation memory matches
      --use-automations                           Run automations for this upload
      --hidden-from-contributors                  Mark newly created keys as hidden from contributors
      --cleanup-mode                              Delete keys/translations not present in the uploaded file
      --custom-translation-status-inserted-keys   Add custom statuses to inserted keys
      --custom-translation-status-updated-keys    Add custom statuses to updated keys
      --custom-translation-status-skipped-keys    Add custom statuses to skipped keys
      --skip-detect-lang-iso                      Skip automatic language detection by filename
      --filter-task-id int                        Apply import results as a part of a task (offline_xliff only)
  -h, --help                                      help for upload
```

### Options inherited from parent commands

```
      --backoff-initial duration     Initial retry backoff (e.g. 400ms, 1s). 0 means library default
      --backoff-max duration         Maximum retry backoff (e.g. 5s, 10s). 0 means library default
      --base-url string              Override Lokalise API base URL
      --config string                Path to YAML config file
      --context-timeout duration     Overall command timeout (e.g. 30s, 2m). 0 disables the timeout (default 2m30s)
      --http-timeout duration        HTTP client timeout (e.g. 30s, 1m). 0 means library default
      --poll-initial-wait duration   Initial wait between polling rounds (e.g. 1s, 2s). 0 means library default
      --poll-max-wait duration       Maximum total wait for polling (e.g. 120s, 5m). 0 means library default
      --project-id string            Lokalise project ID
      --retries int                  Number of retries after the first attempt. -1 means library default (default -1)
      --token string                 Lokalise API token
      --user-agent string            User-Agent header (default "lokex-cli/dev")
```

### SEE ALSO

* [lokex-cli](lokex-cli.md)	 - CLI for uploading and downloading files with Lokalise

###### Auto generated by spf13/cobra on 22-Apr-2026
